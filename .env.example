# ================================================
# PDF Tool - Environment Variables Template
# ================================================
# このファイルをコピーして .env.local を作成し、
# 実際の値を設定してください。
#
# コピーコマンド例:
# cp .env.example .env.local
#
# ================================================

# ------------------------------------------------
# アプリケーション設定
# ------------------------------------------------

# ログインユーザー名（平文）
APP_USERNAME=

# ログインパスワードのハッシュ（bcrypt, cost=12）
# 生成方法: https://bcrypt-generator.com/ でパスワードをハッシュ化
# または Go で: bcrypt.GenerateFromPassword([]byte("your_password"), 12)
APP_PASSWORD_HASH=

# セッション署名鍵（128bit以上のランダム文字列）
# 生成方法: openssl rand -hex 32
SESSION_SECRET=

# ------------------------------------------------
# サーバー設定
# ------------------------------------------------

# APIサーバーのポート番号
PORT=8080

# Ginのモード (debug / release / test)
GIN_MODE=debug

# CORS許可オリジン（カンマ区切りで複数指定可）
# 例: http://localhost:5173,http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:5173

# ------------------------------------------------
# ファイル制限
# ------------------------------------------------

# 単一ファイルの最大サイズ（バイト）
# デフォルト: 104857600 (100MB)
MAX_FILE_SIZE=104857600

# 単一ファイルの最大ページ数
# デフォルト: 200
MAX_PAGES=200

# ジョブの有効期限（分）
# デフォルト: 10
JOB_EXPIRE_MINUTES=10

# Redis 接続先 (Asynq / 進捗管理)
# 例: redis://127.0.0.1:6379/0
QUEUE_REDIS_URL=redis://127.0.0.1:6379/0

# 同期 → 非同期へ切り替えるファイルサイズとページ数の閾値
# 例: 50MB, 120ページ
ASYNC_THRESHOLD_BYTES=52428800
ASYNC_THRESHOLD_PAGES=120

# Ghostscript 実行ファイルのパス (圧縮用)
GHOSTSCRIPT_PATH=gs

# 結果ファイルのベースURL (GCS等を使用する場合)。空の場合は /api/jobs/{id}/download を返却
JOB_RESULT_BASE_URL=

# ------------------------------------------------
# GCP設定（本番環境用 - ローカル開発では不要）
# ------------------------------------------------

# GCPプロジェクトID
GCP_PROJECT=

# Google Cloud Storageバケット名
GCS_BUCKET=

# サービスアカウント（署名URL発行用）
SERVICE_ACCOUNT=
